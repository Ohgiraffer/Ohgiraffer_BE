-- ============================================================
-- chat_channel 보강
-- 1. name 컬럼 NULL 허용으로 변경
--    DM 채널에서 이름 미지정 시, 저장은 null로 두고 조회 시점에
--    상대방 이름으로 대체 표시하기 위함 (양쪽 사용자에게 서로 다른 이름이
--    보여야 하므로 컬럼 값을 한쪽 기준으로 고정 저장할 수 없음)
-- 작성일: 2026-08-10
-- ============================================================

ALTER TABLE `chat_channel`
    MODIFY COLUMN `name` VARCHAR(255) NULL COMMENT '채팅방 이름 (DM 미지정 시 null, 조회 시점에 상대방 이름으로 대체)';