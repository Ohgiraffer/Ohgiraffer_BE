-- 공지에서 AI 일정 등록을 이미 했는지 여부.
--
-- 요구사항이 1회성이다. 한 번 등록하면 공지 상세의 AI 일정 등록 컴포넌트가 다시 뜨지 않는다.
-- 공지 본문이 나중에 수정돼도 이 값은 그대로 둔다. 이미 등록한 일정과 겹쳐 쌓이는 것을 막는다.
--
-- 등록된 캘린더 일정과 공지를 잇는 컬럼은 두지 않는다. 요구사항이 "공지를 수정하거나
-- 삭제해도 캘린더에 등록된 일정에는 영향이 없다" 이므로, 연결을 남겨도 쓸 데가 없다.
ALTER TABLE `notice`
    ADD COLUMN `is_calendar_registered` BOOLEAN NOT NULL DEFAULT FALSE COMMENT 'AI일정등록여부';
